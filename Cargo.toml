[package]
name = "cargo-terse"
version = "0.1.0"
edition = "2021"
description = "Concise cargo output for AI-assisted workflows"
license = "MIT"

[dependencies]
lexopt = "0.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toon-format = "0.2"
